import { FocusInvalidDirective } from './focus-invalid.directive';

describe('FocusInvalidDirective', () => {
  it('should create an instance', () => {
    const directive = new FocusInvalidDirective();
    expect(directive).toBeTruthy();
  });
});
