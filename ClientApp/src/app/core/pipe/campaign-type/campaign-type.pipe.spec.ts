import { CampaignTypePipe } from './campaign-type.pipe';

describe('CampaignTypePipe', () => {
  it('create an instance', () => {
    const pipe = new CampaignTypePipe();
    expect(pipe).toBeTruthy();
  });
});
