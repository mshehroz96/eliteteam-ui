import { ResumeLockedPipe } from './resume-locked.pipe';

describe('ResumeLockedPipe', () => {
  it('create an instance', () => {
    const pipe = new ResumeLockedPipe();
    expect(pipe).toBeTruthy();
  });
});
