import { ResumeUnLockedPipe } from './resume-unlocked.pipe';

describe('ResumeUnLockedPipe', () => {
  it('create an instance', () => {
    const pipe = new ResumeUnLockedPipe();
    expect(pipe).toBeTruthy();
  });
});
